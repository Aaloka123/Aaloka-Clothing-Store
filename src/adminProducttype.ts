export type AdminProduct = {
  id: string;
  name: string;
  price: string;
  img: string;
  description?: string;
  category?: string;
  stock?: string;
  isActive?: boolean;
  createdAt?: string;
  updatedAt?: string;
};
